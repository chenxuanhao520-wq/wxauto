# =====================================================
# Wxauto 智能客服中台 - 环境变量配置
# 云原生架构：Supabase + pgvector + AI Gateway
# =====================================================

# ============== Supabase 配置（必需）==============
# 从 Supabase Dashboard 获取：Settings > API
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# ============== AI 模型配置（至少配置一个）==============
# 通义千问（推荐：复杂推理）
QWEN_API_KEY=your_qwen_api_key

# 智谱 GLM（推荐：极简问答，免费）
GLM_API_KEY=your_glm_api_key

# OpenAI（可选）
OPENAI_API_KEY=your_openai_api_key

# DeepSeek（可选）
DEEPSEEK_API_KEY=your_deepseek_api_key

# Claude（可选）
ANTHROPIC_API_KEY=your_anthropic_api_key

# 文心一言（可选）
ERNIE_API_KEY=your_ernie_api_key
ERNIE_SECRET_KEY=your_ernie_secret_key

# Gemini（可选）
GEMINI_API_KEY=your_gemini_api_key

# 月之暗面（可选）
MOONSHOT_API_KEY=your_moonshot_api_key

# ============== 嵌入服务配置（可选）==============
# 智谱 AI（推荐：中文最佳）
ZHIPUAI_API_KEY=your_zhipuai_api_key

# ============== 其他配置 ==============
# 调试模式
DEBUG=false

# 日志级别：DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# 服务端口
PORT=8000

# ============== 已移除配置 ==============
# Pinecone（已替换为 Supabase pgvector）
# PINECONE_API_KEY=（已弃用）
# PINECONE_ENVIRONMENT=（已弃用）
# PINECONE_INDEX_NAME=（已弃用）
